# Non-functional requirement blocks

This directory contains reusable constraints and deployment strategies.
Currently available:

- `cloud_native_deployment_strategy.md`

When adding new artefacts, capture observability budgets, latency envelopes, and
resiliency strategies so downstream SRS can inherit them verbatim.
